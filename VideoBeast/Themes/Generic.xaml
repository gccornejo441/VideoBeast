<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VideoBeast">

	<Style x:Key="PlaylistListViewItemStyle" TargetType="ListViewItem">
		<Setter Property="Padding" Value="0" />
		<Setter Property="Margin" Value="0,2" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="BorderBrush" Value="Transparent" />

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid>
						<Border x:Name="RowRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">

							<ContentPresenter
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
						</Border>

						<VisualStateManager.VisualStateGroups>

							<!-- Pointer / Pressed -->
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />

								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="RowRoot.Background" Value="#1AFFFFFF" />
										<Setter Target="RowRoot.BorderBrush" Value="#33FFFFFF" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="RowRoot.Background" Value="#33FFFFFF" />
										<Setter Target="RowRoot.BorderBrush" Value="#55FFFFFF" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<!-- Selection -->
							<VisualStateGroup x:Name="SelectionStates">
								<VisualState x:Name="Unselected" />

								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="RowRoot.Background" Value="#222222" />
										<Setter Target="RowRoot.BorderBrush" Value="#666666" />
									</VisualState.Setters>
								</VisualState>

								<VisualState x:Name="SelectedUnfocused">
									<VisualState.Setters>
										<Setter Target="RowRoot.Background" Value="#222222" />
										<Setter Target="RowRoot.BorderBrush" Value="#666666" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

							<!-- Now Playing (Data-bound state trigger) -->
							<VisualStateGroup x:Name="NowPlayingStates">
								<VisualState x:Name="NotNowPlaying" />

								<VisualState x:Name="NowPlaying">
									<VisualState.StateTriggers>
										<!-- DataContext is your item (VideoListItem) -->
										<local:BoolStateTrigger IsActive="{Binding IsNowPlaying}" />
									</VisualState.StateTriggers>

									<!-- Only set BORDER so selection background still wins -->
									<VisualState.Setters>
										<Setter Target="RowRoot.BorderBrush" Value="#4C89FF" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>

						</VisualStateManager.VisualStateGroups>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
